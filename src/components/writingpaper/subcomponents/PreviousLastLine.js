import React from 'react';

class PreviousLastLine extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      //state here
    }
  }

  render() {
    //variables, logic and so on here
      return (
        <div>
          [the last line of what the previous person wrote]
        </div>
      )
    }
  }

export default PreviousLastLine;
